def in_range(i, v, e)
  i <= v && v <= e
end

# define RAND_DOUBLE (double(rand()%10000)/10000.0)
# define round(X) static_cast<int>(floor(X+0.5))
# define square(X) (X) * (X)
# define cube(X) (X) * (X) * (X)
